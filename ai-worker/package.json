{
  "name": "yb-briefbot-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "devDependencies": {
    "wrangler": "^4.124.0"
  }
}
